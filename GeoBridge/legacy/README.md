# legacy

Artifacts from the Mac-host era of the bridge (launchd `ai.geo.bridge` + `install.sh`), retired on 2026-08-03.

The bridge now runs on the VM `garime` as the systemd unit `garime-bridge.service`, exposed via `tailscale serve`; tokens live in `/etc/garime/`. See `../CONTRACT.md` § Deployment.
