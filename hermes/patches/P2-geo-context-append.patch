diff --git a/gateway/run.py b/gateway/run.py
index 2672ab43e..6226cf4b3 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -9401,6 +9401,11 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                 "message": message_text[:500],
             }
             await self.hooks.emit("agent:start", hook_ctx)
+            if hook_ctx.get("geo_context"):
+                context_prompt = (
+                    f"{context_prompt}\n\n{hook_ctx['geo_context']}"
+                    if context_prompt else str(hook_ctx["geo_context"])
+                )
 
             # Run the agent
             agent_result = await self._run_agent(
